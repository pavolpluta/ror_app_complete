<%= render 'shared/errors', obj: @building %>

<%= simple_form_for([:admin, @building], :html => {class: "form-horizontal custom-width center-block", role: "form"}) do |f| %>
  <div class="form-inputs">
    <%= f.input :title, label: "Name of a Building", autofocus: true %>
    <%= f.input :code, placeholder: "Example: 1.1" %>
  </div>

  <div class="form-actions text-center">
    <%= f.submit class: "btn btn-primary btn-lg" %>
  </div>
<% end %>

<div class="help-block">
  <%= link_to "Return to all buildings", admin_buildings_path, class: "pull-left" %>
</div>